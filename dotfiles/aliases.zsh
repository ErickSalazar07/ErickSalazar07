# aliases for zsh

alias vim='gvim -v' # use gvim on the terminal to be able to use + clipboard/register
alias ls='ls -X --group-directories-first --color=auto'
alias ll='ls -lhX --group-directories-first --color=auto'
alias cp='cp -ivr' # recursive copy by default
alias mv='mv -iv'
